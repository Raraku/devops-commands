# Commands relating to cloudSQL

- Create cloud SQL instance  
  `gcloud sql instances create <instance-name> --tier=db-n1-standard-2 region=<region>`

- Connect SQL instance  
  `gcloud sql connect <instance-name>`
